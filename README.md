# SEAD Identity System

This project implements the SEAD Identity System. Its purpose is to provide stable links between foreign systems and SEAD while preserving SEAD's existing relational primary keys. The design distinguishes between entities and value objects: stable identity is tracked for entities, while value objects are treated as part of aggregate state. The current document set separates functional requirements, system design, assessment, and implementation-oriented appendix material.

